set-option -g status-position top
set-option -g set-titles on

setw  -g monitor-activity on

# Plugins
set -g @plugin 'christoomey/vim-tmux-navigator'

# Mouse
set -g mouse "on"

# Bindings
# Prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Window Splits
unbind '"'
unbind %
unbind h
unbind j
unbind k
unbind l
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Easy reloads
bind r source-file ~/.config/tmux/.tmux.conf 

# Directions
# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l


# Pane zoom
bind -n M-z resize-pane -Z

# # Colors
# set -g mode-style "fg=#0c0c0c,bg=#2d4f67"
# set -g message-style "fg=#0c0c0c,bg=#2d4f67"
# set -g message-command-style "fg=#0c0c0c,bg=#2d4f67"
# set -g pane-border-style "fg=#5ec8d8" 
# set -g pane-active-border-style "fg=#5ec8d8"
# set -g status "on"
# set -g status-justify "left"
# set -g status-style "fg=#2d4f67,bg=#2a2a37"
# set -g status-left-length "100"
# set -g status-right-length "100"
# set -g status-left-style NONE
# set -g status-right-style NONE
# set -g status-left "#[fg=#dcd7ba,bg=#957fb8,bold] #S #[fg=#957fb8,bg=#2a2a37,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=#223249,bg=#2a2a37,nobold,nounderscore,noitalics]#[fg=#dcd7ba,bg=#223249] %Y-%m-%d  %I:%M %p #[fg=#957fb8,bg=#223249,nobold,nounderscore,noitalics]#[fg=#000000,bg=#957fb8,bold] #h "
# setw -g window-status-activity-style "underscore,fg=#7b7c7e,bg=#2a2a37"
# setw -g window-status-separator ""
# setw -g window-status-style "NONE,fg=#7b7c7e,bg=#2a2a37"
# setw -g window-status-format "#[fg=#2a2a37,bg=#2a2a37,nobold,nounderscore,noitalics]#[default] #I  #W #(~/.config/tmux/scripts/truncate_path.sh #{pane_current_path} 16) #F"
# setw -g window-status-current-format "#[fg=#2a2a37,bg=#76946a,nobold,nounderscore,noitalics]#[fg=#000000,bg=#76946a,nobold] #I  #W #(~/.config/tmux/scripts/truncate_path.sh #{pane_current_path} 16) #F #[fg=#76946a,bg=#2a2a37,nobold,nounderscore,noitalics]"
#
# Free Escape
set -g escape-time 0

# RGB Terminal
set -g default-terminal "screen-256color"
set -ga terminal-overrides ',xterm-256color:Tc'

# Focus events
set-option -g focus-events on
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'folke/tokyonight'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
