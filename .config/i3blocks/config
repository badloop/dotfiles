# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=$SCRIPT_DIR/$BLOCK_NAME/$BLOCK_NAME
separator_block_width=15
markup=none

[music]
command=playerctl -p cider metadata -f "{{xesam:artist}} - {{xesam:title}}"
interval=5
signal=10
align=center
min_width=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
separator=false

[weather]
command=sh $HOME/.config/i3/scripts/weather.sh
interval=120
align=center

[volume]
interval=persist

[cpu_usage]
min_width=100.00%
interval=5
align=right

[memory]
interval=5

[battery]

[time]
command=date '+%Y-%m-%d %I:%M%p'
interval=5

